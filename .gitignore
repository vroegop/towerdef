.claude/settings.local.json
.claude/worktrees/
.DS_Store

# dev-only deps (e.g. jsdom for tools/hud-smoke.js) — never committed; the game ships build-free
node_modules/
package.json
package-lock.json
